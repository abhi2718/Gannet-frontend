/** The stylised gannet bird mark used in the navbar logo. */
import Image from "next/image";
export function GannetBirdIcon() {
  return <Image src="/logo.svg" alt="Company Logo" width={80} height={80} />;
}

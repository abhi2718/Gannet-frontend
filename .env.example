# Base URL for the Gannet REST API, including the `/api` mount.
# The client (src/lib/api/client.ts) prepends this to every endpoint path.
# For local development, copy this file to `.env.local` and point it at your API:
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api

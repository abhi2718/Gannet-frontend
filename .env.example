# Base URL for the backend API. The React Query hooks in src/lib/query/hooks
# currently resolve local mock data; set this and flip each hook to `apiGet`
# once the real endpoints are available.
NEXT_PUBLIC_API_BASE_URL=
